/** @type {import("prettier").Config} */
const config = {
  plugins: ["prettier-plugin-tailwindcss"],
  proseWrap: "always",
  semi: true,
  singleQuote: false,
  trailingComma: "all",
};

export default config;
